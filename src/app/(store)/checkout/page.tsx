// Checkout.
export default function CheckoutPage() {
  return (
    <main>
      <h1>Checkout</h1>
      {/* TODO: elegir método de pago (efectivo / SPEI). */}
      {/* TODO: si SPEI, mostrar CLABE + referencia y permitir subir comprobante. */}
      {/* TODO: elegir entrega (aula del vendedor por defecto / punto de entrega). */}
      {/* TODO: confirmar pedido y crear el pedido en estado pendiente_pago. */}
    </main>
  );
}
