// Calendario/listado de drops.
export default function DropsPage() {
  return (
    <main>
      <h1>Drops</h1>
      {/* TODO: calendario y listado de drops (próximos, activos, finalizados). */}
      {/* TODO: cuenta regresiva por drop y actualización en tiempo real (realtime). */}
    </main>
  );
}
