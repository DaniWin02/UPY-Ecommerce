// Pantalla de acceso (Ágora) — login Google @uni + OTP por correo
export default function AuthLoginPage() {
  return (
    <main>
      <h1>Acceso</h1>
      {/* TODO: Auth.js — botón "Continuar con Google" (restringido a dominios @uni) */}
      {/* TODO: Auth.js — opción "Enviar enlace por correo" (OTP) */}
    </main>
  );
}
// Fin: pantalla de acceso.
